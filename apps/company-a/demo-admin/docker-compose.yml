services:
  demo-admin-frontend:
    build:
      context: ./frontend
    container_name: demo-admin-frontend
    restart: always
    networks:
      - pandora-net

  demo-admin-backend:
    build:
      context: ./backend-yii
    container_name: demo-admin-backend
    restart: always
    volumes:
      - ./backend-yii:/var/www/html
    networks:
      - pandora-net

networks:
  pandora-net:
    external: true
