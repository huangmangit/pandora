services:
  demo-php74:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: demo-php74
    restart: always
    volumes:
      - ./public:/var/www/html
    networks:
      - pandora-net

networks:
  pandora-net:
    external: true
